{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gptdash.dev/protocol/v1/c2s/game-join.schema.json",
  "title": "game:join (client → server)",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "sessionCode": { "type": "string", "minLength": 4, "maxLength": 8 },
    "name": { "type": "string", "minLength": 1, "maxLength": 40 }
  },
  "required": ["sessionCode", "name"]
}

