{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gptdash.dev/protocol/v1/c2s/game-resume.schema.json",
  "title": "game:resume (client → server)",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "sessionCode": { "type": "string", "minLength": 4, "maxLength": 8 },
    "role": { "type": "string", "enum": ["host", "player"] },
    "token": { "type": "string", "minLength": 10 }
  },
  "required": ["sessionCode", "role", "token"]
}

